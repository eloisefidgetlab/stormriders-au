<div class="featured-brands-wrapper all-brands-page">
  <h2>
    {{ section.settings.title }}
    {% if section.settings.text != blank %}
      <p>{{ section.settings.text }}</p>
    {% endif %}
  </h2>
  <div class="brand-logo-wrapper">
    {%- for block in section.blocks -%}
      <div class="brand-logo-item">
        <a {% if block.settings.page-link == blank %} class="no-link" {% endif %} href="{% if block.settings.page-link != blank %} {{block.settings.page-link}} {% endif %}">
          <img src="{{ block.settings.image | img_url: 'medium' }}" alt="brand-image">
        </a>
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Brands Page",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Information"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Trading site link"
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Item",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "brand",
          "label": "Brand name"
        },
        {
          "type": "url",
          "id": "page-link",
          "label": "Custom page link"
        }
      ]
    }
  ]
}
{% endschema %}